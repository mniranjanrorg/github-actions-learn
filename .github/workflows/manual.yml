name: Trigger manually
on:
  workflow_dispatch:
    inputs:
      param1:
        description: Write the words describing humans
        required: true
        default: ascetic
jobs:
  show-info:
    runs-on: ubuntu-latest
    steps:
      - name: Print echo
        run: |
          echo "Print the word {{ github.event.inputs.param1 }}"
  
